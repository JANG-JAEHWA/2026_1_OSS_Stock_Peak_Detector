# 3. Design

**Project Title:** 주식 고점 판독기 (Stock Peak Detector)
**Student Info:** 22110615 / 장재화 / jaehwajang@yu.ac.kr

---

### [ Revision history ]

| Revision date | Version # | Description | Author |
| :--- | :--- | :--- | :--- |
| 2026.06.05 | 1.0.0 | Design 문서 초안 작성 (Class, Sequence, State Machine) | 장재화 |

---

### = Contents =
1. [Introduction](#1-introduction)
2. [Class diagram](#2-class-diagram)
3. [Sequence diagram](#3-sequence-diagram)
4. [State machine diagram](#4-state-machine-diagram)
5. [Implementation requirements](#5-implementation-requirements)
6. [Glossary](#6-glossary)
7. [References](#7-references)

---

## 1. Introduction

본 문서는 '주식 고점 판독기(Stock Peak Detector)' 시스템의 이전 단계인 Analysis 문서에서 분석한 요구사항과 도메인 모델을 바탕으로, 시스템의 기능들을 실제 소프트웨어로 '어떻게 구현할 것인가'를 구체화한 Design 문서입니다.

본 시스템은 별도의 무거운 RDBMS를 배제하고 로컬 파일 시스템(CSV)과 사전 학습된 딥러닝 모델 가중치 파일 기반으로 동작하는 가벼운 구조를 가집니다. 이를 위해 객체지향적 관점에서 설계된 Class diagram을 통해 정적인 구조를 정의하고, Sequence diagram과 State machine diagram을 통해 객체 간의 동적 상호작용과 시스템의 상태 변화를 모델링하여 실제 구현(Implementation)을 위한 명확한 청사진을 제시합니다.

---

## 2. Class diagram

본 시스템은 사용자의 입력을 처리하는 UI, 전체 흐름을 제어하는 Controller, 데이터를 관리하는 Manager, 인공지능 추론을 담당하는 Predictor 등으로 모듈화되어 설계되었습니다.

<img width="591" height="541" alt="image" src="https://github.com/user-attachments/assets/721b4a69-9e37-41c3-8c45-4788224fcd24" />


| Class Name | Attributes | Methods | Description |
| :--- | :--- | :--- | :--- |
| **SystemController** | - `is_running: bool`<br>- `current_stock: Stock` | + `main()`<br>+ `run_analysis(stock_code: str)` | 시스템의 전체 실행 흐름을 제어하고 각 모듈 간의 데이터를 중계하는 중심 클래스입니다. |
| **ConsoleUI** | - `theme_color: str` | + `get_user_input() -> str`<br>+ `display_result(prob: float)`<br>+ `show_error(msg: str)` | 터미널 환경에서 사용자로부터 종목 정보를 입력받고, 분석 결과를 텍스트로 시각화합니다. |
| **StockDataManager** | - `raw_data_path: str`<br>- `processed_data_path: str` | + `fetch_data(stock_code: str)`<br>+ `apply_minmax_scaler(data: DataFrame)`<br>+ `save_to_csv(data: DataFrame)` | 외부 API로부터 OHLCV 데이터를 수집하고, 모델 입력에 맞게 정규화(MinMaxScaler)하여 로컬에 저장합니다. |
| **PeakPredictor** | - `model_weight_path: str`<br>- `lookback_window: int` | + `load_lstm_model()`<br>+ `predict_peak(data: DataFrame) -> float` | 학습된 LSTM 모델(.h5/.pth)을 로드하여 시계열 데이터를 입력받고, 0~100% 사이의 고점 확률을 연산합니다. |
| **Stock** | - `code: str`<br>- `name: str`<br>- `market_type: str` | + `get_info()`<br>+ `update_last_analyzed_date()` | 분석 대상이 되는 주식 종목의 기본 메타데이터(종목명, 코드 등)를 담는 엔티티 클래스입니다. |

---

## 3. Sequence diagram

사용자가 특정 종목의 분석을 요청하고, 데이터를 수집/전처리하여 인공지능 모델이 추론한 뒤 결과를 반환하기까지의 동적 호출 순서를 나타냅니다.

<img width="689" height="482" alt="image" src="https://github.com/user-attachments/assets/53e71b3c-8ea4-4d7c-b8af-b89fea111285" />



**[ 동작 흐름 설명 ]**
1. **요청 입력:** `User`가 `ConsoleUI`를 통해 종목명 또는 코드를 입력합니다.
2. **분석 지시:** `ConsoleUI`는 `SystemController`의 `run_analysis(stock_code)`를 호출하여 분석을 시작합니다.
3. **데이터 수집:** `SystemController`는 `StockDataManager`에게 `fetch_data(stock_code)`를 요청하고, Manager는 외부 `External Data API`와 통신하여 최신 OHLCV 데이터를 가져옵니다.
4. **데이터 전처리:** 가져온 원본 데이터는 `apply_minmax_scaler()`를 통해 0과 1 사이의 값으로 정규화되고 로컬 CSV에 저장됩니다.
5. **AI 추론 요청:** `SystemController`는 전처리된 데이터를 `PeakPredictor`의 `predict_peak(data)` 인자로 전달합니다.
6. **확률 연산:** `PeakPredictor`는 내부적으로 로드해 둔 LSTM 가중치 모델을 기반으로 고점일 확률(%)을 계산하여 반환합니다.
7. **결과 출력:** `SystemController`가 확률 값을 `ConsoleUI`의 `display_result(prob)`로 전달하고, 사용자에게 최종 수치가 화면에 출력됩니다.

---

## 4. State machine diagram

프로그램이 실행되는 동안 시스템 내부가 어떤 상태를 거치며 전환되는지를 나타냅니다.

<img width="579" height="454" alt="image" src="https://github.com/user-attachments/assets/c68b40ed-d910-4af4-8e26-00f5e50f914c" />


**[ 상태 전환 설명 ]**
* **Idle (대기 상태):** 시스템이 켜진 후 사용자의 종목 입력을 기다리는 기본 상태입니다.
* **Fetching Data (데이터 수집 상태):** 유효한 종목이 입력되면 FinanceDataReader 등을 통해 최신 시장 데이터를 수집하는 상태입니다. 통신 실패 시 Error 상태로 빠집니다.
* **Preprocessing (데이터 전처리 상태):** 수집된 데이터를 딥러닝 모델의 규격에 맞추어 스케일링(MinMaxScaler)하는 상태입니다.
* **Inferencing (AI 추론 상태):** LSTM 모델에 전처리된 시계열 텐서를 입력하여 계산을 수행하는 상태입니다. 연산이 성공적으로 완료되면 Result Display 상태로 전환됩니다.
* **Result Display (결과 출력 상태):** 도출된 고점 확률을 콘솔에 출력합니다. 출력이 끝나면 다시 새로운 분석을 위해 Idle 상태로 돌아갑니다.
* **Error State (오류 상태):** 존재하지 않는 종목 입력, 네트워크 연결 끊김 등의 문제가 발생했을 때 경고 메시지를 띄우고 다시 Idle 상태로 복귀합니다.

---

## 5. Implementation requirements

본 시스템을 원활하게 구현하고 실행하기 위한 하드웨어 및 소프트웨어 요구사항입니다.

### 5.1. S/W platform requirements
* **Operating System:** Windows 10 이상 / macOS 11 이상 / Linux (Ubuntu 20.04 이상)
* **Implementation Language:** Python 3.8 이상
* **Deep Learning Framework:** TensorFlow(Keras) 2.x 또는 PyTorch 1.x 이상
* **Required Libraries:** Pandas, NumPy, Scikit-learn (MinMaxScaler 용), FinanceDataReader

### 5.2. H/W platform requirements
* **CPU:** Intel Core i5 / AMD Ryzen 5 이상 권장
* **RAM:** 최소 4GB 이상 (10년 치 이상의 OHLCV 데이터를 메모리에 적재하기 위함)
* **Storage:** 10GB 이상의 여유 공간 (수십 개 종목의 CSV 파일 및 수 MB~수십 MB 크기의 가중치 모델 파일 `.h5` / `.pth` 보관 목적)
* **Network:** 외부 주식 데이터 API 호출을 위한 상시 인터넷 연결 필수

---

## 6. Glossary

| Term | Description |
| :--- | :--- |
| **Class Diagram** | 시스템의 논리 설계를 위한 클래스들의 존재와 그들의 정적인 관계, 속성, 메서드를 도식으로 정의한 다이어그램입니다. |
| **Sequence Diagram** | 객체 간의 동적 상호작용을 시간의 흐름(순서)을 중심으로 모델링한 다이어그램입니다. |
| **State Machine Diagram** | 시스템이나 객체가 생명주기 동안 겪게 되는 상태의 변화와, 그 상태를 전이시키는 이벤트를 나타낸 다이어그램입니다. |
| **LSTM (Long Short-Term Memory)** | 과거의 데이터를 순차적으로 처리하며, 중요한 과거 정보는 오래 기억하고 불필요한 정보는 잊어버리도록 설계된 순환 신경망(RNN). |
| **MinMaxScaler** | 데이터의 최솟값을 0, 최댓값을 1로 변환하여 데이터의 단위를 통일하는 전처리 기법으로, 본 시스템에서 주가 단위 편차 문제를 해결합니다. |
| **OHLCV** | 주식 차트를 구성하는 5가지 기본 데이터 (Open: 시가, High: 고가, Low: 저가, Close: 종가, Volume: 거래량). |

---

## 7. References

1. **FinanceDataReader User Guide**: 한국 거래소(KRX) 및 글로벌 주식 데이터 수집. (https://github.com/FinanceData/FinanceDataReader)
2. **TensorFlow/Keras API Reference**: LSTM 신경망 레이어 구현 및 시계열(Time-series) 데이터 처리. (https://www.tensorflow.org/api_docs)
3. **Sepp Hochreiter & Jürgen Schmidhuber (1997)**: "Long Short-Term Memory", Neural Computation.
4. **Scikit-learn Documentation**: `MinMaxScaler`를 활용한 데이터 정규화(Normalization).
